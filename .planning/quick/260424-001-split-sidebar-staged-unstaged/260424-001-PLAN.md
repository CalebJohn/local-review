---
mode: quick
quick_id: "260424-001"
slug: split-sidebar-staged-unstaged
description: Split sidebar into staged and unstaged sections
tasks: 3
---

# Quick Task: Split Sidebar into Staged and Unstaged Sections

## Task 1: Update App data model for two-section sidebar

**Files:** `src/app.rs`
**Action:** Add `SidebarSection` enum (Staged/Unstaged). Replace single `files: Vec<FileEntry>` with `staged_files` and `unstaged_files` Vecs. Add `sidebar_section` field. Update MoveUp/MoveDown to cross section boundaries. Update `load_diff_for_selected` to use section context for choosing HEAD-vs-index or index-vs-workdir diff. Update `refresh_files` to rebuild both lists. Update StageFile/UnstageFile/StageHunk/UnstageHunk to reference correct section.
**Verify:** `cargo check` passes
**Done:** App supports two-section navigation with correct diff selection

## Task 2: Update UI to render split sidebar

**Files:** `src/ui.rs`
**Action:** Split sidebar area vertically into two sub-areas (proportional to file count). Render "Staged" list and "Unstaged" list with separate blocks. Highlight active section border. Handle empty sections gracefully.
**Verify:** `cargo check` passes
**Done:** Sidebar renders two distinct sections

## Task 3: Update mouse handling and fix tests

**Files:** `src/main.rs`, `src/app.rs`
**Action:** Update mouse click translation to account for two sidebar sections (staged area vs unstaged area positions). Fix existing tests for new data model.
**Verify:** `cargo test` passes
**Done:** Mouse clicks correctly target the right section and file
